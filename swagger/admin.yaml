paths:
  /admin:
    get:
      tags:
        - Admin
      summary: 관리자 페이지 로그인(접속)
      parameters:
        - in: query
          name: code
          required: true
          schema:
            type: string
      responses:
        200:
          description: 인증 성공
        400:
          description: 잘못된 인증코드
        500:
          description: 서버 에러
